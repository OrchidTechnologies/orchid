name := Orchid
domain := com.example

teamid := XXX
codesign := "iPhone Developer: XXX ($(teamid))"

provision := 

# xcode := DEVELOPER_DIR=/Applications/Xcode-XXX.app

cflags += '-DORCHID_INFURA="XXX"'
cflags += '-DORCHID_ALCHEMY="XXX"'

cflags += '-DORCHID_USERNAME="XXX"'
cflags += '-DORCHID_PASSWORD="XXX"'
