name := Orchid
domain := com.example

teamid := XXX
codesign := "iPhone Developer: XXX ($(teamid))"

provision := 

# export DEVELOPER_DIR := /Applications/Xcode-XXX.app
# suffix := -XXX

cflags += '-DORCHID_INFURA="XXX"'
cflags += '-DORCHID_ALCHEMY="XXX"'

#MAKEFLAGS += --output-sync=line
